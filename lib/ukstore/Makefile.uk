$(eval $(call addlib_s,libukstore,$(CONFIG_LIBUKSTORE)))

CINCLUDES-$(CONFIG_LIBUKSTORE)   += -I$(LIBUKSTORE_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKSTORE) += -I$(LIBUKSTORE_BASE)/include

LIBUKSTORE_SRCS-y += $(LIBUKSTORE_BASE)/store.c
